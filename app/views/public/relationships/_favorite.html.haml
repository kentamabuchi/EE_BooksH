- if Relationship.find_by(user_id: current_user.id, follow_id: user.id)
  = link_to "お気に入り", user_relationships_path(user), method: :delete, remote: true, class: "UShow__favorite"
- else
  = link_to "お気に入り", user_relationships_path(user), method: :post, remote: true, class: "UShow__favorite--none"
