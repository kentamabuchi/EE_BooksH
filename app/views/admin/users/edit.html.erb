<div class="UserPage">
  <div class="UserPage__container">
    
    <section class="BEdit">
        <div class="BEdit__action__return">
            <%= link_to "戻る", admin_user_path(@user), class: "BEdit__btn__return" %>
        </div>
        <div class="BEdit__heading">
            <h2>会員編集</h2>
        </div>
        
        <div class="BEdit__contents">
            
            <div class="BEdit__BookImage">
                <div class="BEdit__ImageTag">
                    <p>現在の画像</p>
                </div>
                <div class="BEdit__image">
                    <%= attachment_image_tag @user, :image, :fill, 300, 300, fallback: "no_image.jpg",size: '300x300' %>
                </div>
            </div>
            
            <div class="BEdit__form">
            <%= form_with model: @user, url: admin_user_path(@user), method: :patch, local: true do |f| %>
                <table class="BEdit__item">
                    <tr>
                        <th class="BEdit__thread"><span class="BEdit__tag">プロフィール画像：</span></th>
                        <td class="BEdit__thread"><%= f.attachment_field :image %></td>
                    </tr>
                    <tr>
                        <th class="BEdit__thread"><span class="BEdit__tag">ニックネーム：</span></th>
                        <td class="BEdit__thread"><%= f.text_field :nick_name %></td>
                    </tr>
                    <tr>
                        <th class="BEdit__thread"><span class="BEdit__tag">メールアドレス：</span></th>
                        <td class="BEdit__thread"><%= f.text_field :email %></td>
                    </tr>
                     <tr>
                        <th class="BEdit__thread"><span class="BEdit__tag">会員ステータス：</span></th>
                        <td class="BEdit__thread">
                            <% if @user.is_active == true %>
                            <%= f.radio_button :is_active, true, checked: "checked" %><%= f.label :is_active, "有効" %>
                            <%= f.radio_button :is_active, false %><%= f.label :is_active, "退会" %>
                            <% else %>
                            <%= f.radio_button :is_active, true %><%= f.label :is_active, "有効" %>
                            <%= f.radio_button :is_active, false, checked: "checked" %><%= f.label :is_active, "退会" %>
                            <% end %>
                        </td>
                    </tr>
                </table>
                <div class="BEdit__action">
                    <%= f.submit "変更", class: "BEdit__btn" %>
                </div>
            <% end %>
            </div>
        </div>
        
    </section>

  </div>
</div>

